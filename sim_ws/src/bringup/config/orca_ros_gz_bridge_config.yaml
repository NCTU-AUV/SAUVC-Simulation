# # Full set of configurations
# - ros_topic_name: "ros_chatter"
#   gz_topic_name: "gz_chatter"
#   ros_type_name: "std_msgs/msg/String"
#   gz_type_name: "gz.msgs.StringMsg"
#   subscriber_queue: 5       # Default 10 if qos_profile is empty, otherwise not set by default
#   publisher_queue: 6        # Default 10 if qos_profile is empty, otherwise not set by default
#   lazy: true                # Default "false"
#   direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
#                             # "GZ_TO_ROS" - Bridge Gz topic to ROS
#                             # "ROS_TO_GZ" - Bridge ROS topic to Gz
#   qos_profile: SENSOR_DATA  # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "camera/bottom/image_raw"
  gz_topic_name: "camera/bottom/image_raw"
  ros_type_name: "sensor_msgs/msg/Image"
  gz_type_name: "gz.msgs.Image"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: GZ_TO_ROS  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "sensors/imu"
  gz_topic_name: "sensors/imu"
  ros_type_name: "sensor_msgs/msg/Imu"
  gz_type_name: "gz.msgs.IMU"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: GZ_TO_ROS  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "sensors/altimeter"
  gz_topic_name: "sensors/altimeter"
  ros_type_name: "ros_gz_interfaces/msg/Altimeter"
  gz_type_name: "ignition.msgs.Altimeter"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: GZ_TO_ROS  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_0/force_N"
  gz_topic_name: "thrusters/thruster_0/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_1/force_N"
  gz_topic_name: "thrusters/thruster_1/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_2/force_N"
  gz_topic_name: "thrusters/thruster_2/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_3/force_N"
  gz_topic_name: "thrusters/thruster_3/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_4/force_N"
  gz_topic_name: "thrusters/thruster_4/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_5/force_N"
  gz_topic_name: "thrusters/thruster_5/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_6/force_N"
  gz_topic_name: "thrusters/thruster_6/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size

- ros_topic_name: "thrusters/thruster_7/force_N"
  gz_topic_name: "thrusters/thruster_7/force_N"
  ros_type_name: "std_msgs/msg/Float64"
  gz_type_name: "ignition.msgs.Double"
  subscriber_queue: 10       # Default 10 if qos_profile is empty, otherwise not set by default
  publisher_queue: 10        # Default 10 if qos_profile is empty, otherwise not set by default
  lazy: false                # Default "false"
  direction: BIDIRECTIONAL  # Default "BIDIRECTIONAL" - Bridge both directions
                            # "GZ_TO_ROS" - Bridge Gz topic to ROS
                            # "ROS_TO_GZ" - Bridge ROS topic to Gz
  qos_profile: 10           # Default is a default-constructed QoS with appropriate queue size
